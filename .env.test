# Test Environment Configuration
# Copy this file and configure with your test database credentials

# Test Database URL
# Use a separate database for testing to avoid affecting development data
TEST_DATABASE_URL=postgresql://localhost:5432/portfolio_test

# Session Configuration (can use simple values for testing)
SESSION_SECRET=test-session-secret-key-for-testing-only

# Node Environment
NODE_ENV=test
