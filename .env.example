# ============================================
# PORTFOLIO APPLICATION - ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and update the values for your environment
# Never commit the .env file to version control!

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# 
# For local development:
#   - Ensure PostgreSQL is installed and running
#   - Create a database named 'portfolio' (or your preferred name)
#   - Update the connection string with your credentials
#
# For production:
#   - Use your production PostgreSQL connection string
#   - Ensure the database is accessible from your server
#
DATABASE_URL=postgresql://localhost:5432/portfolio

# ============================================
# SERVER CONFIGURATION
# ============================================
# Port number for the application server
# Default: 5000
# Note: Replit uses port 5000 by default
PORT=5000

# Node environment (development, production, test)
# This affects security settings like cookie security and CORS
NODE_ENV=development

# ============================================
# SESSION & AUTHENTICATION
# ============================================
# Secret key for encrypting session cookies
# IMPORTANT: Change this to a strong, random string in production!
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Security notes:
#   - Must be at least 32 characters long
#   - Should be different for each environment
#   - Never share or commit this value
#
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# ============================================
# FILE UPLOAD CONFIGURATION
# ============================================
# Maximum file size for uploads (in bytes)
# Default: 5242880 (5MB)
# Note: Currently hardcoded in server/middleware/upload.ts
# Future enhancement: Make this configurable via environment variable
# UPLOAD_MAX_FILE_SIZE=5242880

# Allowed file types for uploads (comma-separated)
# Default: jpeg,jpg,png,gif,webp
# Note: Currently hardcoded in server/middleware/upload.ts
# Future enhancement: Make this configurable via environment variable
# UPLOAD_ALLOWED_TYPES=jpeg,jpg,png,gif,webp

# Upload directory path (relative to project root)
# Default: uploads/
# Note: Currently hardcoded in server/middleware/upload.ts
# Future enhancement: Make this configurable via environment variable
# UPLOAD_DIR=uploads

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Update DATABASE_URL with your PostgreSQL credentials
# 3. Generate a strong SESSION_SECRET (see command above)
# 4. Adjust PORT if needed (default 5000 works for most setups)
# 5. Set NODE_ENV to 'production' when deploying
# 6. Run database migrations: npm run db:push
# 7. (Optional) Seed the database: npm run db:seed
#
# For more information, see docs/guides/DEPLOYMENT.md
